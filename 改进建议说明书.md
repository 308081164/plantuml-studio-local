# PlantUML 本地工作室 — 提示词、知识库与 Agent 流程改进建议说明书

> **文档性质**：在**不修改当前源代码**的前提下，对现有设计进行深度评估，并给出可落地的演进策略与前后对比。  
> **适用对象**：产品/架构负责人、实现工程师、后续迭代评审。  
> **基线版本说明**：以当前 `electron-main.mjs` 与 `vendor/kb/PlantUML-Agent-Knowledge-Base.md` 所体现的行为为准；若未来实现与本说明书不一致，以代码为准。

---

## 1. 文档目的与范围

### 1.1 目的

- 将**提示词（System / User / 辅助提示）**、**知识库（KB）构建与注入**、**Agent 调用流程（规划—生成—校验—重试）**三者放在同一框架下审视。  
- 给出**改进后的目标策略**，并与**现状**逐项对比，说明**变化点**与**预期收益**（质量、成本、可维护性、可观测性）。

### 1.2 范围

| 包含 | 不包含 |
|------|--------|
| 制图主链路（自然语言 → PlantUML → 本地校验） | 授权/License、安装包、非 Agent 的 UI 逻辑 |
| 项目制图链路（清单 → 规划 → 拼包 → 制图） | 替换模型厂商（仍以「可配置 OpenAI 兼容 API」为抽象） |
| KB 单文件 Markdown 形态与注入方式 | 具体编程语言与框架选型（实现阶段再定） |

---

## 2. 现状摘要（基线）

### 2.1 知识库（KB）

- **载体**：`PlantUML-Agent-Knowledge-Base.md`，含 YAML 元数据、章节表、`retrieval_hints` 等，**文档结构规范、可读性强**。  
- **运行时加载**：整文件读入，按**固定字符上限**（约 4 万字符）**从开头截断**后作为「摘录」拼入 System Prompt。  
- **语义**：实质为**静态长上下文注入**，并非按查询向量检索的 RAG；YAML 中的「检索策略」主要依赖模型自行阅读理解。

### 2.2 提示词（Prompt）

- **主 System**：角色 + 输出契约（只输出可渲染 PlantUML 等）+ **条件拼接**的「国内高校模式」大段规则与示例 + **知识库摘录**字符串。  
- **项目模式**：在主 System 末尾追加「项目模式」说明（索引 + 选文件 + 缺口用 note 等）。  
- **规划器 System**：独立 `PROJECT_PLANNER_SYSTEM`，要求输出严格 JSON（路径列表 + rationale）。  
- **修正轮 User**：附加 PlantUML 错误文本、`PLANTUML_RETRY_HINT`（如 `title` 与字面 `\n`）、以及「禁止与上一轮提取结果逐字相同」的强化提示。  
- **后处理**：对模型输出做 `extractPlantumlFromModelText`；在开启国内高校模式时对源码做 `applyChinaUnivModeIfNeeded`（`@startchen` 豁免）。

### 2.3 Agent 调用流程

**简单制图（`runAgentPipeline`）**

1. 读取配置与 KB 摘录 → 组装 `system`。  
2. 循环至多 `1 + maxRetries` 轮：  
   - 调用 DeepSeek（首轮与修正轮 `user` 文案不同，温度不同）。  
   - 抽取 PlantUML → 国内高校后处理 → 本地 `plantumlRenderCheck`（如 `-tpng`）。  
   - 成功则返回；失败则携带错误与当前源码进入下一轮。

**项目制图（`runAgentPipelineWithProject`）**

1. 本地扫描生成清单（JSONL，有截断与忽略规则）。  
2. **规划阶段**：专用 system + 清单 user → 解析 JSON 路径；失败则**启发式回退**。  
3. 按预算组装「头部 + 文件包 + 尾部」大块 user 正文，做 token 粗算上限检查。  
4. 与简单制图相同的「生成—抽取—后处理—校验—重试」闭环。

**优势（基线已具备的）**

- **校验闭环**：以 PlantUML 真机渲染为判据，优于纯模型自评。  
- **规划与制图分离**：降低单次 JSON + 超长 PlantUML 混写难度。  
- **重复输出检测**：缓解「多轮字符数相同」类无效修复。  
- **专用规划 System**：边界清晰，利于单独迭代与评测。

---

## 3. 基线主要问题与风险（为何需要改进）

### 3.1 知识库与「检索」表述不一致

- 文档宣称「检索锚点 / 检索策略」，运行时却是**前缀截断**，后半章节在 KB 膨胀时可能**永不进入上下文**。  
- **风险**：特定图种（文档后部）的规则缺失 → 幻觉或老语法；**维护者误以为已实现 RAG**。

### 3.2 System 过长与规则优先级不显式

- 国内高校专规 + 大段示例 + KB 摘录叠加，**总 token 高**，且重要约束可能落在「中间段」，与注意力分布相关文献中的风险一致。  
- **通用**「信息不足可用 note」与 **@startchen**「严禁 note」等若未在提示中写明**冲突裁决顺序**，模型偶发选错边。

### 3.3 KB 注入与图种/任务弱相关

- ~~同一 `readKnowledgeBaseSnippet()` 用于简单制图与项目制图，**未按** `@startuml` / `@startchen` / 甘特等**裁剪章节**~~（v2.0.0-pro.1 起已由 `buildKnowledgeInjection` + 意图路由替代，见 §11）。

### 3.4 Agent 流程可增强之处（仍属策略层）

- **校验维度单一**：默认 PNG 管线；部分语法问题仅在 SVG/特定 skin 下暴露（可选增强点）。  
- **修正轮信息结构**：错误 + 全量源码对长图重复送，**成本**与**注意力分散**随轮次上升。  
- **规划阶段**：Planner 与制图模型**无共享「中间态 schema」**（如「意图标签」），不利于后续多工具协作。  
- **可观测性**：日志已有字符串级记录；若要做持续优化，建议补充**结构化指标**（见 §7）。

---

## 4. 改进后的目标策略（总览）

以下描述**目标态**（说明书级），实现时可分阶段落地。

### 4.1 知识库：三层 + 可选检索

| 层级 | 内容 | 注入策略 |
|------|------|----------|
| **L0 护栏** | 输出契约、@start/@end 要求、与渲染器版本绑定的「硬约束」摘要（极短） | **每请求必带**，固定于 system 前部或独立 developer 段 |
| **L1 任务包** | 与当前意图相关的 KB 章节（如仅活动图 / 仅 @startchen） | **按意图或 @start 类型选择**，控制 token 上限 |
| **L2 深度参考** | 全文 KB 或低频章节 | **按需检索**（关键词 / 向量 / 手工路由表）或用户显式「展开参考」 |

**可选检索（RAG）**：将 KB 按标题切块，带 `diagram_types[]`、`keywords[]`、`version_min` 元数据；请求时先规则路由再向量 top‑k，最后拼接进 system 或 tool 结果。

### 4.2 提示词：结构重组与冲突裁决

- **固定块顺序建议**：`角色与输出契约` → `硬约束与禁止项` → `模式专规（国内高校 / Chen，二选一或带条件）` → `与通用规则的冲突说明（专规优先）` → `KB 摘录` → `本轮任务说明`。  
- **显式写清**：「当本段与上文冲突时，以本节为准」置于 **Chen / 活动图专节** 顶部。  
- **示例与规则分离**：大段示例尽量移至「附录引用」或「少样例 + 指向 KB 章节」，减少 system 体积。

### 4.3 Agent 流程：状态机 + 分级修正 + 可选多判据

```text
意图分类（轻量调用或规则） → [规划：仅项目模式] → 生成 → 抽取 → 后处理
        → 校验（PNG 必选 + SVG/语法检查可选）
        → 失败则「结构化错误摘要」+「最小相关片段」进入修正轮
        → 成功或达上限
```

- **意图分类**：输出离散标签（`activity_cn_univ` / `chen_er` / `sequence` / `other`），用于 **L1 KB 选取** 与 **是否附加国内高校后处理**（减少误用 `activity` 头）。  
- **分级修正**：首轮给完整上下文；修正轮优先附 **错误行 ±N 行** + **符号表摘要**，全量源码作为次要块，降低重复 token。  
- **Planner 输出 schema 扩展**（可选）：`{"paths":[],"rationale":"","diagram_guess":"","risk_notes":""}`，制图阶段可消费 `diagram_guess` 以选 KB。

---

## 5. 前后策略对比总表

| 维度 | 现状（Before） | 目标（After） | 变化要点 | 预期优势 |
|------|----------------|---------------|----------|----------|
| KB 注入 | 整篇前缀截断约 4 万字符 | L0 短护栏 + L1 按图种/意图选章 + 可选 L2 检索 | 从「一刀切」到「分层与相关」 | 提高命中率、降低无关噪声与 token |
| 与文档一致性 | 文档写「检索」、实现为截断 | 文档区分「设计检索策略」与「实现管线名称」；或实现真检索 | 语义对齐 | 降低团队误解、便于审计 |
| System 结构 | 单一大字符串 | 分块有序 + 冲突裁决句 + 示例瘦身 | 结构化 | 降低违规率、减轻 lost-in-the-middle |
| 国内高校 / Chen | 条件拼接在同一 system | 专规独立块 + 写明与通用 note 规则的优先级 | 显式优先级 | 减少 Chen 与活动图规则串味 |
| 项目模式 user | 单块超长正文 | 保持可读前提下「摘要索引 + 选中文件」；修正轮压缩重复 | 分轮策略 | 降本、减轻模型遗忘首部约束 |
| 规划阶段 | Planner 失败 → 启发式回退 | 保留回退 + 可选「二次规划」或规则白名单路径 | 弹性增强 | 大仓库下路径质量更稳 |
| 校验 | 以 PNG 为主 | PNG 必选 + 可选第二判据（如语法/ SVG） | 多维可选 | 提前暴露部分语法类问题 |
| 观测与迭代 | 以文本日志为主 | 增加结构化字段与离线题集 | 可度量 | 持续回归、可对比版本 |

---

## 6. 分域详细说明

### 6.1 知识库（KB）演进

**Before**

- 单文件高质量 Markdown + YAML。  
- 运行时：顺序读文件 → 截断 → 拼进 system。

**After（建议）**

1. **内容层**：保持单仓单文件也可，但建议为每章增加机器可读标签（YAML 或 HTML 注释块）：`applies_to: [activity, chen]`、`min_plantuml: "1.2024.5"`。  
2. **分发层**：  
   - **路由表**（无需向量）：`@startchen` → 仅注入 §13 陈氏 + §11 错误 + L0；`@startuml activity` + 国内高校开关 → 注入活动图专章 + L0。  
   - **向量检索**（KB 再变大时）：chunk 粒度建议「二级标题为界」，检索 query = 用户自然语言 + 意图标签。  
3. **版本层**：KB `doc_id` 与 `plantuml-*.jar` 版本写入 L0 一句，减少跨版本语法幻觉。

**优势小结**：相关性 ↑、无效 token ↓、与 PlantUML 版本对齐可审计。

### 6.2 提示词（Prompt）演进

**Before**

- 角色、输出、国内高校大段、KB 摘录、项目说明在同一 system 串联。  
- 修正提示以常量和 user 拼接为主。

**After（建议）**

1. **硬约束短表**（10～30 行量级）：永远置顶，例如：必须闭包、禁止仅 Markdown、国内高校开关状态、`@startchen` 时禁止 note 等。  
2. **模式专规**：仅在与意图一致时注入；内部首行写「**本节覆盖与本节冲突的通用表述**」。  
3. **KB 摘录**：明确标注「以下为节选 §x–§y，非全文」。  
4. **修正轮**：除错误原文外，增加**结构化错误摘要**（行号、规则类型、建议操作枚举），可由本地预处理从 PlantUML 输出中提取，减少模型解析 stderr 的负担。  
5. **温度策略**：保留「首轮低、修正略高」；可在「重复上一轮提取结果」时**短暂提高**或切换 `n` 采样（若 API 支持），作为现有 dup 检测的补充说明写进运维文档。

**优势小结**：违规率与重复修复率预期下降；提示词变更可灰度（先改 L0/L1）。

### 6.3 Agent 调用流程演进

**Before**

- 简单链路：单 system 固定 + 多轮同一 system。  
- 项目链路：Planner → 大块 user → 同一制图闭环。

**After（建议）**

1. **前置轻量意图分类**（可选用小模型/规则/极短 prompt）：输出标签，驱动 KB 路由与后处理开关，**不必**增加用户步骤。  
2. **制图闭环不变内核**：仍以 PlantUML 校验为黄金标准；在闭环外增加「可选 SVG 校验」作为早筛。  
3. **修正轮 payload 分级**：Round 1 全量；Round 2+ 以「错误相关片段」为主，全量源码折叠为附件式块或哈希引用（策略描述级）。  
4. **Planner 与制图对齐**：将 `diagram_guess` 或 `paths` 类型统计回写到制图 user 首部一句，减少「选了 API 文件却画活动图」的错配。  
5. **失败出口标准化**：达最大重试后，返回 `last_err` + `建议用户操作`（缩短 API Key、简化需求、关闭国内高校模式等）枚举，利于产品与客服脚本。

**优势小结**：成本可控、长项目下修正更有效、与后续多 Agent/工具扩展兼容。

---

## 7. 实施路线（建议分阶段）

| 阶段 | 内容 | 依赖 | 风险 |
|------|------|------|------|
| **Phase 0** | 文档与提示词文案：冲突裁决、章节引用说明、编号与示例整理；**零代码**也可先做评审版 | 无 | 低 |
| **Phase 1** | KB 路由表 + 按 `@start` / 开关选章 + L0 短护栏；仍可不引入向量库 | 低开发量 | 中：需回归题集 |
| **Phase 2** | 向量检索或嵌入式 DB；修正轮分级 payload | 运维与存储 | 中高：需监控质量 |
| **Phase 3** | 多判据校验、意图小模型、Planner schema 扩展 | 成本与延迟 | 高：需 AB 测试 |

建议**每阶段**配套：**固定题集回归**（活动图、Chen、时序、故意错误语法、长项目节选）与**线上日志抽样**（成功轮次、失败类型分布）。

---

## 8. 指标体系（建议纳入后续运维）

| 指标 | 含义 | 用途 |
|------|------|------|
| 首轮通过率 | 第 1 轮即 plantuml ok 的比例 | 衡量提示词 + KB 相关性 |
| 平均修正轮数 | 成功样本上的 round 分布 | 衡量修正策略是否有效 |
| 重复提取率 | dup 检测触发次数 / 修正轮数 | 衡量 extract + 温度策略 |
| Planner 回退率 | 启发式替代 Planner 的比例 | 衡量规划 prompt 与解析鲁棒性 |
| Token / 请求 | system+user 总字符粗算 | 成本与是否需压缩 payload |
| 违规模式计数 | 如误用 `start`/`stop`、Chen 使用 note | 衡量专规优先级是否足够 |

---

## 9. 风险与非目标（说明书边界）

**风险**

- 过度压缩 KB 可能导致**长尾语法**缺失 → 需用题集平衡。  
- 意图分类错误会导致**选错 KB 包** → 需回退到「宽包」或双包并集策略。  
- 多判据校验增加本地耗时 → 应作为可选开关。

**非目标（本说明书不强制）**

- 不指定必须用某一向量数据库或某一云厂商。  
- 不要求一次性完成 Phase 3；允许长期保持 Phase 1 为「性价比最优解」。

---

## 10. 结论

当前实现已具备 **「强校验闭环 + 规划与制图分离 + 防重复提取」** 等工程上较成熟的 Agent 骨架；**知识库文档本身结构规范**。主要改进空间集中在：**KB 注入从「前缀截断」演进为「分层与可选检索」**、**System 提示从「长单块」演进为「有序块 + 冲突裁决」**、**修正轮从「全量重复送」演进为「分级 payload」**，以及配套的**文档—实现语义对齐**与**指标化回归**。

按 **Phase 0 → 1 → 2** 渐进落地，可在控制开发量的前提下，系统性提升 **合规率、可维护性、成本与可观测性**。

---

**文档版本**：1.0  
**撰写说明**：基于对 `electron-main.mjs` 与 `vendor/kb/PlantUML-Agent-Knowledge-Base.md` 的阅读理解整理；若后续代码结构变化，请同步修订本说明书对应章节。

---

## 11. 实现状态（v2.0.0-pro.1 与本文对齐说明）

以下与 **§4～§6** 目标策略对应，说明当前代码已落地范围及与说明书的差异。

| 说明书要点 | 实现位置 / 行为 |
|------------|----------------|
| L0 护栏 + L1 按意图选 KB 章 | `scripts/kb-inject.mjs`：`buildKnowledgeInjection` 按二级标题分块，按 `classifyDiagramIntent` 路由章节号并控制总字符；无匹配时前缀回退。`electron-main.mjs` 将 `inj.l0` 与节选注入 `buildAgentSystemPrompt`。 |
| 意图分类 | `scripts/agent-intent.mjs`：`classifyDiagramIntent`、`shouldApplyChinaUnivPostProcess`；国内高校后处理仅在允许意图时执行，避免误改时序/类图等。 |
| System 分块与冲突裁决 | `buildAgentSystemPrompt`：L0 → 角色契约 → 规则优先级句 → 知识库节选标注 → 国内高校专规（含「专规优先声明」）；国内高校节内示例编号 **8️⃣** 已与 **7️⃣** 区分。 |
| Planner schema 扩展 | `PROJECT_PLANNER_SYSTEM` 与 `plannerUser` 要求 `diagram_guess`、`risk_notes`；`parsePlannerPaths` 解析；`buildProjectUserBlockParts` 写入首轮 user 头部。 |
| 分级修正轮 payload | `buildAgentRetryUserContent`：自第 3 轮起（`round >= 2`）附错误行附近切片 + 折叠全文。 |
| PNG + 可选 SVG | `plantumlRenderCheckWithOptionalSvg`：环境变量 `UML_MASTER_DUAL_RENDER=1` 或 `true` 时在 PNG 通过后追加 `-tsvg`；**仅记录警告**，不推翻 PNG 成功结果。 |
| 结构化指标 | Agent 日志中追加 `[metrics] {...}` 行：`intent`、`kbChars`、`plannerFallback`、`dualSvgOk` 等。 |

**未纳入或简化处（相对 Phase 2～3）**：未引入向量库；未实现「失败出口枚举」独立 API 字段；指标仅日志字符串，未接外部遥测。若后续升级，可在此表续写版本列。
